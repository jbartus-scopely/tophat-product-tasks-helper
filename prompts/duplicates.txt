Here are {{taskCount}} tasks (ID and Description only):

```csv
{{csv}}
```

Known duplicate IDs: {{knownDuplicates}}

Find groups of tasks whose descriptions refer to the same topic, overlap significantly, or where one is part of the other. Ignore status — focus only on description similarity.

Assign each cluster a short name (e.g. "Board roll timing", "Album reward bugs").

Output a CSV block:

```csv
Task ID,AI Description,AI Priority,AI Action,AI Notes,AI Group
<ID>,<original description>,<High|Mid|Low>,<action>,<explanation>,<cluster name>
```

Rules:
- Only include tasks in a cluster (2+ tasks on the same topic). Skip unique tasks.
- AI Description: copy the original description as-is.
- AI Priority: High = best version; Mid = partial overlap; Low = weaker duplicate.
- AI Action: Keep, Merge, or Discard.
- AI Notes: which task ID it overlaps with and why. Use semicolons not commas.
- AI Group: cluster name. Same cluster = same exact value.
- No commas inside fields; use semicolons.

After the CSV, one sentence per cluster summarizing the overlap.
