Here is the current product task backlog ({{taskCount}} tasks):

```csv
{{csv}}
```
{{warnings}}

User question: {{userPrompt}}

Your response MUST include a CSV block in this exact format:

```csv
Task ID,AI Description,AI Priority,AI Action,AI Notes,AI Group
<ID>,<original task description>,<High|Mid|Low>,<action>,<recommendation>,<group>
```

Rules for the CSV:
- Include the most relevant tasks for the user's question. Aim for 10-20 tasks.
- AI Description: use the original task description from the backlog as-is.
- AI Priority: High, Mid, or Low (your assessment of this task's importance).
- AI Action: MUST be one of: Take Next, Consider, Skip.
  - Take Next — strongly recommended; high-value; actionable now.
  - Consider — worth evaluating; good candidate but not top priority.
  - Skip — not recommended right now (blocked; low value; not ready).
- AI Notes: a short reason or context. Use semicolons not commas.
- AI Group: the group this task belongs to. MUST be one of: Timings, UI/UX, MRG, Album, SD/BH, Social, Cosmetics, Rolling, QW, Bug, Tournaments, Bots, Delight, FTUE, Networth, Other.
- Do NOT use commas inside fields; use semicolons instead.

After the CSV block, provide a brief rationale section explaining your recommendations.
