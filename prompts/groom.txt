Here are the TRIAGE tasks to groom ({{taskCount}} tasks){{groupFilter}}:

```csv
{{triageCsv}}
```

For context, here are the active backlog tasks (non-TRIAGE, non-Live, non-HOLD) that are already prioritized or being worked on ({{activeCount}} tasks):

```csv
{{activeCsv}}
```

Your job is to groom the TRIAGE tasks. For each one, decide:
- **TODO candidate**: Should this move to TODO? It's clear, actionable, and valuable.
- **Discard — duplicate**: Similar to an active task already being worked on or prioritized. Name the overlapping task ID.
- **Discard — too big**: This is not a product task; it's a full feature/initiative that needs its own discovery or epic. Explain briefly.
- **Keep in TRIAGE**: Not enough info to decide; needs more context or scoping.

Your response MUST include a CSV block in this exact format:

```csv
Task ID,AI Description,AI Priority,AI Action,AI Notes,AI Group
<ID>,<short description max 120 chars>,<High|Mid|Low>,<action>,<recommendation>,<group>
```

Rules for the CSV:
- One row per TRIAGE task. Use the original Task ID.
- AI Description: a simplified, shorter version (max 120 chars).
- AI Priority: High, Mid, or Low (your suggested priority if it were promoted to TODO).
- AI Action: the recommended action. MUST be one of: TODO, Keep Triage, Discard, Merge, Hold.
  - TODO — task is actionable and should move forward.
  - Keep Triage — needs more info or discussion before committing.
  - Discard — obsolete; duplicated; or not worth pursuing (too big; etc.).
  - Merge — should be merged with another related task (reference the target task ID in AI Notes).
  - Hold — has value but is intentionally deferred (blocked; wrong timing; etc.).
- AI Notes: a short reason or context. When action is Discard or Merge; reference the overlapping task ID. Use semicolons not commas.
- AI Group: suggest which group this task belongs to. MUST be one of: Timings, UI/UX, MRG, Album, SD/BH, Social, Cosmetics, Rolling, QW, Bug, Tournaments, Bots, Delight, FTUE, Networth, Other. Pick the best fit based on the task description.
- Do NOT use commas inside fields; use semicolons instead.

After the CSV block, provide:
1. Tasks recommended to move to TODO, grouped by theme if possible.
2. Tasks that overlap with active work — list the TRIAGE task ID and the active task ID it duplicates.
3. Tasks that are too big for a product task and should become features/epics.
4. Tasks that need more context before a decision can be made.
